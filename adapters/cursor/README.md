# Cursor SDK Adapter Mapping

Expose Siesta via Cursor dynamic tools:

- One `siesta` tool with `operation` enum
- Manifest as context resource for agent introspection
- Session handles managed by adapter between tool calls

See [MCP adapter](../mcp/README.md) for operation mapping — semantics are identical.
